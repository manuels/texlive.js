\ProvidesPackage{beraserif}[2004/01/30 (WaS)]
\RequirePackage{keyval}
\define@key{Fve}{scaled}[.9]{%
  \def\fve@Scale{#1}}
\def\ProcessOptionsWithKV#1{%
  \let\@tempc\relax
  \let\Fve@tempa\@empty
  \@for\CurrentOption:=\@classoptionslist\do{%
    \@ifundefined{KV@#1@\CurrentOption}%
    {}%
    {%
      \edef\Fve@tempa{\Fve@tempa,\CurrentOption,}%
      \@expandtwoargs\@removeelement\CurrentOption
        \@unusedoptionlist\@unusedoptionlist
    }%
  }%
  \edef\Fve@tempa{%
    \noexpand\setkeys{#1}{%
      \Fve@tempa\@ptionlist{\@currname.\@currext}%
    }%
  }%
  \Fve@tempa
  \let\CurrentOption\@empty
}
\ProcessOptionsWithKV{Fve}
\AtEndOfPackage{%
  \let\@unprocessedoptions\relax
}
\renewcommand{\rmdefault}{fve}
\normalfont
\endinput
%%
%% End of file `beraserif.sty'.
